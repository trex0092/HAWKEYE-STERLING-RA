{
  "_README": "Additional / curated sanctions lists loaded ONLY by scripts/sanctions-screen.mjs (not by the watcher's strict loader). A source may be remote (`url` + `parser`) or in-repo curated (`file`, parser 'curated'). Set `enabled:false` to keep a source configured but skipped. A remote source that fails or parses 0 names, or a curated list with no entries, degrades coverage (reported loudly) — never a silent all-clear.",
  "sources": [
    {
      "id": "uae-eocn-local-terrorist",
      "name": "UAE EOCN — Local Terrorist List",
      "jurisdiction": "United Arab Emirates",
      "file": "data/eocn-local-terrorist-list.json",
      "type": "curated",
      "parser": "curated",
      "enabled": true,
      "_note": "EOCN publishes this only as PDF/Excel behind bot protection — no free machine-readable feed. Maintain the names in data/eocn-local-terrorist-list.json from the official EOCN download (https://www.uaeiec.gov.ae). While unpopulated it flags 'coverage incomplete', never a clean result."
    },
    {
      "id": "ca-sema",
      "name": "Canada — Consolidated Autonomous Sanctions (SEMA, XML)",
      "jurisdiction": "Global",
      "url": "https://www.international.gc.ca/world-monde/assets/office_docs/international_relations-relations_internationales/sanctions/sema-lmes.xml",
      "type": "xml",
      "parser": "xml",
      "enabled": true,
      "_note": "Best-effort generic-XML parse; if the structure changes it degrades visibly rather than silently."
    },
    {
      "id": "ch-seco",
      "name": "Switzerland — SECO sanctions list (XML)",
      "jurisdiction": "Global",
      "file": "data/seco-curated-list.json",
      "type": "curated",
      "parser": "curated",
      "enabled": false,
      "_note": "SECO's portal (sesam.search.admin.ch) serves an HTML page to automated GETs, not raw XML (verified 2026-06-27 customer screen — parsed 0 names), so it is maintained as a curated in-repo list instead. POPULATE data/seco-curated-list.json from the official download, then set enabled:true to screen SECO directly. While empty it stays disabled (no false clear, no permanent 'degraded'). The dedicated `seco` XML parser and the curated parser are both unit-tested. Until populated, SECO designations are covered indirectly via the UN/EU/OFAC lists already loaded."
    },
    {
      "id": "au-dfat",
      "name": "Australia — DFAT Consolidated List",
      "jurisdiction": "Global",
      "file": "data/dfat-curated-list.json",
      "type": "curated",
      "parser": "curated",
      "enabled": false,
      "_note": "DFAT bot-gates regulation8_consolidated.xlsx (HTTP 404 to automated fetches; verified 2026-06-27 customer screen), so it is maintained as a curated in-repo list. POPULATE data/dfat-curated-list.json from the official download, then set enabled:true to screen DFAT directly. While empty it stays disabled (no false clear, no permanent 'degraded'). The dependency-free XLSX reader in sanctions-match.mjs (ZIP + sharedStrings + worksheet via node:zlib) and the curated parser are both unit-tested; the XLSX path can be used instead if a fetchable URL is ever confirmed. Until populated, DFAT designations are covered indirectly via the UN/OFAC/EU lists already loaded."
    },
    {
      "id": "wb-debarred",
      "name": "World Bank — Listing of Ineligible (Debarred) Firms & Individuals",
      "jurisdiction": "Global",
      "url": "",
      "type": "csv",
      "parser": "curated",
      "enabled": false,
      "_note": "Debarment (not a sanctions designation) — a useful EDD signal for procurement-linked entities. Published as HTML/JSON at https://www.worldbank.org/en/projects-operations/procurement/debarred-firms with no stable bulk CSV; maintain as a curated file or confirm a machine-readable endpoint before enabling."
    }
  ]
}
