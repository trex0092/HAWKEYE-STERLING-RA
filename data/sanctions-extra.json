{
  "_README": "Additional / curated sanctions lists loaded ONLY by scripts/sanctions-screen.mjs (not by the watcher's strict loader). A source may be remote (`url` + `parser`) or in-repo curated (`file`, parser 'curated'). Set `enabled:false` to keep a source configured but skipped. A remote source that fails or parses 0 names, or a curated list with no entries, degrades coverage (reported loudly) — never a silent all-clear.",
  "sources": [
    {
      "id": "uae-eocn-local-terrorist",
      "name": "UAE EOCN — Local Terrorist List",
      "jurisdiction": "United Arab Emirates",
      "file": "data/eocn-local-terrorist-list.json",
      "type": "curated",
      "parser": "curated",
      "enabled": true,
      "_note": "EOCN publishes this only as PDF/Excel behind bot protection — no free machine-readable feed. Maintain the names in data/eocn-local-terrorist-list.json from the official EOCN download (https://www.uaeiec.gov.ae). While unpopulated it flags 'coverage incomplete', never a clean result."
    },
    {
      "id": "ca-sema",
      "name": "Canada — Consolidated Autonomous Sanctions (SEMA, XML)",
      "jurisdiction": "Global",
      "url": "https://www.international.gc.ca/world-monde/assets/office_docs/international_relations-relations_internationales/sanctions/sema-lmes.xml",
      "type": "xml",
      "parser": "xml",
      "enabled": true,
      "_note": "Best-effort generic-XML parse; if the structure changes it degrades visibly rather than silently."
    },
    {
      "id": "ch-seco",
      "name": "Switzerland — SECO sanctions list (XML)",
      "jurisdiction": "Global",
      "url": "https://www.sesam.search.admin.ch/sesam-search-web/pages/downloadXmlGesamtliste.xhtml?lang=en",
      "type": "xml",
      "parser": "seco",
      "enabled": false,
      "_note": "DISABLED after live verification (2026-06-27 customer screen): the host is reachable but a plain GET to this `.xhtml` endpoint returns the JSF HTML page wrapper, not the raw 'Gesamtliste' XML, so the parser extracts 0 names — leaving the screen permanently 'degraded'. The dedicated `seco` parser (sanctions-match.mjs, nested <name>/<name-part>/<value>) is correct and unit-tested; re-enable only once a confirmed direct-download XML URL (or a JSF session POST flow) is verified ON THE RUNNER. Until then SECO designations are covered indirectly via the UN/EU/OFAC lists already loaded."
    },
    {
      "id": "au-dfat",
      "name": "Australia — DFAT Consolidated List",
      "jurisdiction": "Global",
      "url": "https://www.dfat.gov.au/sites/default/files/regulation8_consolidated.xlsx",
      "type": "xlsx",
      "parser": "dfat",
      "enabled": false,
      "_note": "DISABLED after live verification (2026-06-27 customer screen): the host resolves and connects but this path returns HTTP 404 to the automated fetch — DFAT appears to bot-protect the file (it serves to browsers only), the same pattern as the UAE EOCN download. The dependency-free XLSX reader in sanctions-match.mjs (ZIP + sharedStrings + first worksheet via node:zlib; collects the name columns) is correct and unit-tested. Re-enable once a confirmed, fetchable URL is verified ON THE RUNNER, or maintain the names in-repo as a curated file (parser 'curated') like the UAE EOCN list. DFAT designations are partly covered indirectly via the UN/OFAC/EU lists already loaded."
    },
    {
      "id": "wb-debarred",
      "name": "World Bank — Listing of Ineligible (Debarred) Firms & Individuals",
      "jurisdiction": "Global",
      "url": "",
      "type": "csv",
      "parser": "curated",
      "enabled": false,
      "_note": "Debarment (not a sanctions designation) — a useful EDD signal for procurement-linked entities. Published as HTML/JSON at https://www.worldbank.org/en/projects-operations/procurement/debarred-firms with no stable bulk CSV; maintain as a curated file or confirm a machine-readable endpoint before enabling."
    }
  ]
}
